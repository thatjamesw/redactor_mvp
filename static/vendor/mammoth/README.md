Local DOCX parsing assets live here so `.docx` support works without any CDN or backend.

Generated files:

- `static/vendor/mammoth/mammoth.browser.min.js`

Refresh these files after dependency updates with:

```bash
npm install
npm run vendor:browser-deps
```
