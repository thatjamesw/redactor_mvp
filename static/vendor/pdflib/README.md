Local PDF assembly assets live here so visually redacted PDFs can be exported without any CDN or backend.

Generated files:

- `static/vendor/pdflib/pdf-lib.min.js`

Refresh these files after dependency updates with:

```bash
npm install
npm run vendor:browser-deps
```
